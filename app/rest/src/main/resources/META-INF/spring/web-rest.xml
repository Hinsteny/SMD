<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://code.alibabatech.com/schema/dubbo
        http://code.alibabatech.com/schema/dubbo/dubbo.xsd"
       default-autowire="byName">

    <dubbo:application name="${app.name}"/>

    <!--<dubbo:registry address="N/A"/>-->
    <dubbo:registry protocol="zookeeper" address="${dubbo.zookeeper.address}"/>

    <dubbo:protocol name="rest" port="${dubbo.rest.port}"/>

    <dubbo:service interface="org.hinsteny.rest.resource.UserResource" ref="userResource" protocol="rest"/>

</beans>